import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appName: 'angular-movies',
  webDir: 'dist/angular-movies',
  bundledWebRuntime: false
};

export default config;
